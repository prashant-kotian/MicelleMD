# MicelleMD Roadmap

Working plan, consolidated from PhD project discussion (2026-09-01/02). Not started yet — this captures the scope and design decisions already made so nothing gets lost before work actually begins, following completion of Paper 3.

## Objective

SurfactantKit computes CPP and aggregation number as **closed-form geometric estimates** from Tanford's tail-volume/length formulas — a fast, analytical approximation, explicitly documented in SurfactantKit itself as "a geometric estimate, not a substitute for aggregation numbers measured directly." MicelleMD is the real-computation complement: *simulate* the same surfactant self-assembling and measure aggregation number, shape, and packing directly from the simulation, rather than estimating it from a single-molecule geometric argument.

This directly strengthens the Paper 3 thesis too: it demonstrates the distinction between "AI recalling/estimating a number" and "actually running the physics" using the project's own tooling as the worked example — SurfactantKit's *estimate* vs. MicelleMD's *simulation* of the identical physical quantity (aggregation number) is a clean, citable illustration of exactly the gap Paper 3's benchmark is built to measure.

## Planned technical approach

- **Method**: coarse-grained MD, MARTINI-style force field (not atomistic — computationally tractable for self-assembly timescales, standard choice in the surfactant-CG-MD literature).
- **Engine**: thin wrapper over an established MD package (GROMACS or OpenMM) — deliberately not a from-scratch MD implementation. The scientific/engineering contribution is the surfactant-specific topology generation and analysis layer, not the integrator.
- **Topology generation**: automated construction of CG surfactant topologies for the surfactant classes already central to this PhD's work — single-chain ionic/nonionic, gemini (two-tail, spacer-linked), and amidoamine-derived architectures specifically (the amide linkage and its hydrogen-bonding behavior is a nontrivial CG-mapping decision worth getting right, not a standard case the usual MARTINI surfactant library covers off the shelf).
- **Simulation**: self-assembly runs starting from random/dispersed initial configuration, standard approach for observing spontaneous micellization in CG-MD.
- **Analysis outputs**:
  - Aggregation number distribution (not just a single mean — real micelles are polydisperse, and reporting the distribution rather than a point estimate is itself a more honest comparison against SurfactantKit's single-number geometric estimate)
  - Micelle shape descriptors: radius of gyration, asphericity/eccentricity
  - Solvent-accessible surface area (SASA)
  - Counterion association/binding degree (a real-computation cross-check against SurfactantKit's `counterion_binding_degree`, which is itself a conductometric-slope-method *estimate*, not a first-principles calculation — same "estimate vs. simulate" comparison pattern)
- **Comparison hooks**: explicit, built-in comparison utility that runs the *same* surfactant through both SurfactantKit's geometric aggregation-number estimate and MicelleMD's simulated result, reporting agreement/disagreement — this comparison IS a core planned deliverable, not an afterthought.

## Open design questions (to resolve when work starts)

- Exact CG mapping scheme for the amidoamine amide linkage (standard MARTINI bead types may not cleanly represent amide hydrogen-bonding behavior — may need a validated custom bead assignment, itself worth literature-checking before hardcoding, per this project's established "verify before hardcode" discipline).
- Which specific surfactants to validate against first — likely reuse the same literature-validated systems already in SurfactantKit's `literature_validation_notes.md` (e.g. SDS, DTAB, CTAB) where independent real aggregation-number measurements already exist (Bales et al. 1998's ~44.8-54.2 range for SDS is a ready-made validation target).
- Compute resource planning — CG-MD self-assembly runs are far more compute-intensive than anything in SurfactantKit; needs its own resource/timeline plan before committing to a run schedule.

## Status

Repo created 2026-09-01 (private, MIT-licensed, `prashant-kotian` account).

**2026-09-02 update — engine plumbing proven working, real physics parameters still pending:**
- OpenMM (the chosen MD engine) installs and runs correctly in this environment.
- `vermouth` (the real MARTINI topology-generation tool, maintained by the actual MARTINI developers) also installs and imports correctly — after fixing a real Windows-specific encoding bug in the package itself (run with `PYTHONUTF8=1`). It bundles genuine MARTINI 3.001 force-field infrastructure, but that bundle is protein/nucleotide-focused out of the box, not surfactant-specific.
- `src/micellemd/cg_model.py`: a CG bead/molecule data model and OpenMM system builder, run and verified end to end — built an 80-bead toy system (20 simple 4-bead linear surfactants), ran 1000 steps of real Langevin dynamics, energy relaxed and stayed numerically stable throughout. **As of 2026-09-03, this now uses the real MARTINI 3 nonbonded parameter set, not placeholders — see the dated update below.**
- **Real GROMACS now working too, on a second (Ubuntu) machine**: connected a second laptop over SSH on the same LAN (existing key-based auth already set up), installed GROMACS via conda (no native Windows path exists — checked directly, no binary, no Chocolatey package). Ran a complete, real pipeline — box creation, solvation (884 SPC waters), topology, preprocessing, and an actual steepest-descent energy minimization that **converged in 8 steps** (potential energy -35202.24 kJ/mol) — using only GROMACS's own bundled data. Full setup, and two real bugs found and fixed along the way (a hard segfault specific to one force-field/water-model pairing, and a cutoff-vs-box-size sizing issue), documented in `GROMACS_SETUP.md`. This means MicelleMD now has genuine access to its originally-named primary engine, not just the OpenMM fallback.
- **Next concrete step (2026-09-03 update — MARTINI parameters now sourced, see below)**: build an actual surfactant topology for GROMACS (via `vermouth`'s mapping machinery or hand-built) and run it through the now-proven GROMACS pipeline in `GROMACS_SETUP.md`, cross-checked against the OpenMM real-MARTINI-3-parameter results now in `cg_model.py`. The CG mapping question for the amidoamine amide linkage still needs resolving.

**2026-09-02 update — real solvated, neutralized, ionic atomistic system, via the GAFF/acpype route (path fully independent of the MARTINI-parameter blocker above):**
- Extended the single-ion-in-vacuum GAFF result to a genuine SDS anion + Na+ counterion + explicit TIP3P water system. Found and fixed a real GROMACS bug along the way: combining acpype's own auto-generated topology with a full AMBER-family force field's water/ion definitions causes an "Invalid order for directive defaults" error, because both sources define their own `[ defaults ]` and GROMACS only allows one in the assembled topology. Fixed by restructuring the include order (force field's `forcefield.itp` first, then the ligand's bare `.itp`, not acpype's full `.top`) and encoded as a reusable, tested function: `atomistic_topology.write_combined_topology()`.
- Full pipeline verified twice end to end (once manually, once purely through the module function on a fresh RDKit embedding): box → solvate (866 waters) → neutralize with 1 Na+ via `genion` → real PME energy minimization, **converged in 149 steps**, potential energy ≈ -37,700 kJ/mol. This is a genuinely electrostatically-complete ionic surfactant system in explicit water, not a placeholder or a vacuum toy.
- This GAFF/atomistic route and the MARTINI/CG route remain complementary, not competing (see the trade-off note in `GROMACS_SETUP.md`): atomistic is now the proven path for real, verifiable single-molecule and small-system physics; CG remains the right choice for large-scale self-assembly once real MARTINI parameters are sourced.

**2026-09-02→03 (overnight) — 20-molecule self-assembly run completed: genuine nucleation into a stable multi-aggregate state, not full micellization.** Full 10 ns atomistic MD (20 SDS anions + 20 Na+ + ~6700 TIP3P waters) ran to completion (9h56m wall time). Real result, honestly characterized (full detail in `GROMACS_SETUP.md`): an initial ~3 ns fission-fusion phase (clusters forming/breaking stochastically) gave way to a genuinely stable nucleation event at t≈3200 ps -- 3 aggregates (sizes 9, 7, 4 molecules) that persisted essentially unchanged for the remaining 6.8 ns, including several real aggregate-aggregate collision events (9-mer and 4-mer repeatedly touching then separating without merging) late in the run. Final frame: still 3 aggregates, sizes {9,7,4}. This is genuine, non-trivial, physically sensible self-assembly dynamics from a fully dispersed start -- not full micellization (real SDS aggregation number ~45-54 vs. this system's largest stable cluster of 9, expected given only 20 total molecules and a comparatively short 10 ns), but a real proof that the atomistic GAFF/acpype pipeline captures genuine self-assembly physics, not just plumbing. Next: scale up system size for a micelle-reachable aggregation number, and/or extend this exact run from its checkpoint to see whether the observed collision-without-fusion pattern eventually resolves into a merge.

**2026-09-02→03 (second overnight extension) -- the collision-without-fusion question answered: yes, it eventually resolves into a real merge.** Extended the exact same run from its `prod.cpt` checkpoint by another 10 ns via `gmx convert-tpr -extend 10000` + `gmx mdrun -cpi` (real GROMACS continuation mechanics, not a fresh restart), reaching 20 ns total (10,000,000 steps, confirmed via the log's own `Finished mdrun` completion line, not assumed). Concatenated the original and extension trajectories with `gmx trjcat -cat` into one continuous 20 ns record (first attempt using `-settime` silently truncated the output to only the first segment -- caught by directly checking the merged file's frame count and time range via MDAnalysis before trusting it, not assumed correct from `trjcat`'s own printed log; `-cat` fixed it, verified by the same direct check afterward: 2001 frames, 0-20000 ps).

Re-ran `atomistic_analysis.py`'s PBC-aware aggregation clustering over the full, now-correctly-concatenated 20 ns trajectory. Result: the {9,7,4} state (already known stable through the original 10 ns) **persisted for a further ~2 ns into the extension** (stable to t≈12000 ps), then at **t≈12490 ps the 9-mer and 4-mer genuinely merged** -- exactly the aggregate pair that had been observed colliding-and-separating without fusing at least 6 times late in the original run. The resulting **{13, 7}** two-aggregate state then held stable, essentially unchanged, for the entire remaining **~7.5 ns** of the run (t=12490 to 19990 ps, confirmed at 500 ps sampling resolution throughout), with the 13-mer's radius of gyration staying in a tight 0.90-0.97 nm band -- not a transient touch-and-separate event like the earlier repeated collisions, a real, kinetically-stable coalescence. Real result, not the largest possible outcome: still short of a single real SDS-scale micelle (Nagg ~45-54) and still two aggregates, not one, but direct, positive evidence that the atomistic GAFF/acpype pipeline can capture genuine aggregate coalescence given enough simulation time, not just nucleation and stasis. The next honest step for a single-micelle-scale result remains what it already was: a larger total molecule count (this 20-molecule system's monomer pool is close to exhausted at 7+13=20), which is exactly the design choice already made independently for the CG production route (150 molecules, see below).

**2026-09-03 — real MARTINI 3 surfactant parameters sourced and wired into `cg_model.py`, resolving the CG-route parameter blocker.** Souza et al. 2021 (Nat. Methods, DOI: 10.1038/s41592-021-01098-3) is the primary citation, but the actual numbers were pulled from the official `martini_v3.0.0.itp` particle-definition file as bundled (unmodified, GROMACS-format) test data in `github.com/maccallumlab/martini_openmm`, a peer-reviewed Martini-in-OpenMM implementation (Biophys. J. 2023). Confirmed the SDS-analog bead assignment against a second independent source (Vainikka et al. 2021, cited in a Frontiers in Materials 2022 SDS paper): 3× `C1` apolar tail beads + 1× `Q4n` anionic sulfate headgroup bead — exactly the architecture this project's `make_linear_surfactant()` already builds. `PLACEHOLDER_BEAD_TYPES` replaced with real self-interaction LJ values (`Q4n`: σ=0.470 nm/ε=5.20 kJ/mol, `C1`: σ=0.470/ε=3.39, `W`: σ=0.470/ε=4.65). Also checked, and it mattered: OpenMM's default Lorentz-Berthelot combining rule does **not** reproduce MARTINI's real cross-species (headgroup↔tail↔water) interactions — epsilon differs by ~2x for both C1-W and C1-Q4n pairs versus the real matrix. Molecule-specific bonded parameters (bond force constants) remain unverified placeholders — out of scope this round, since the base `.itp` file only carries nonbonded interaction parameters, not per-molecule bonded terms.

**2026-09-03 (later same day) — real bug found and fixed in the cross-type nonbonded scheme above, before it got far: `NonbondedForce` per-pair exceptions ignore the cutoff distance.** The original fix used `addException()` to override cross-type LJ per particle pair — correct idiom for a small, fixed set of special pairs (e.g. 1-2 bonded exclusions), but OpenMM exceptions are evaluated directly regardless of distance, so every cross-type pair in the *entire* system (not just nearby ones) got a permanent, always-on LJ interaction. Found while building a production-scale system with explicit water (see below) and cross-checking its `CustomNonbondedForce`-based scheme against the exception-based one on an identical small system: energies disagreed by ~0.2-0.3%, small enough to be tempting to wave off as noise. Root-caused properly instead: enumerated the actual exceptions OpenMM had stored (`NonbondedForce.getExceptionParameters()`) and checked each one's real particle separation against the 1.1 nm cutoff — **16 of 24 exceptions on a small 2-molecule/11-bead test system were for pairs already >1.1 nm apart**, and a fully independent hand-calculation (no OpenMM force objects at all, raw pairwise LJ + reaction-field Coulomb) confirmed the `CustomNonbondedForce` answer was the physically correct one, not the exception-based one. Fixed by replacing exceptions with one `CustomNonbondedForce` (per-particle type index + `Discrete2DFunction` lookup table, OpenMM's standard "NBFIX"-style mechanism, evaluated via its normal cutoff-respecting neighbor-list code) for LJ, plus a separate charge-only `NonbondedForce` for Coulomb — `build_openmm_system()` and the new `build_solvated_openmm_system()` now share this one correct scheme via `_make_nonbonded_forces()`. Also found and fixed a second, smaller issue in the same pass: `CustomNonbondedForce` defaults to no analytic LJ long-range/dispersion correction, unlike `NonbondedForce`, which defaults to including one (~0.3% of the original discrepancy) — now explicitly enabled to match standard MD practice. `examples/verify_solvated_system_energy.py` is the regression test that catches this class of bug going forward, kept as an ongoing check, not a one-time bug report.

**Corrected smoke-test numbers, superseding the ones reported above (same 20-molecule/1000-step system, now with the fixed scheme)**: initial PE is deterministic and reproducible at **2633.17 kJ/mol** (vs. the earlier, bug-affected 2628.53) since system construction has no randomness; final PE and the exact aggregation snapshot are NOT reproducible run-to-run (`context.setVelocitiesToTemperature()` draws from an unseeded random velocity distribution, so each run follows a different stochastic Langevin trajectory) -- repeated runs gave final PE in the 528-547 kJ/mol range and aggregation distributions like {1:9, 2:4, 3:1} or {1:5, 2:3, 3:3}, all qualitatively similar (mostly monomers/dimers with at most one trimer, no dramatic distortion from the bug in practice at this small/short scale). The qualitative conclusion (early-stage clustering signal, not yet a production result) is unchanged; the underlying mechanism producing it is now actually correct. (Worth noting for later: this run-to-run stochasticity means a real production run's specific aggregation outcome will also need repeat runs or a long enough single trajectory to trust as representative, not a one-shot result.)

**Explicit water added — `build_solvated_openmm_system()`, the real missing piece for genuine self-assembly.** The smoke-test system above has no solvent at all — surfactant tails have nothing to avoid, only a headgroup to (weakly) prefer less, a materially wrong/weaker driving force than the real hydrophobic effect. New function fills the box with explicit MARTINI water beads at the real density (4 real H2O per CG bead, computed from real water's known density and Avogadro's number, not hardcoded — ~8.6 beads/nm³) after placing surfactants on a coarse 3D lattice, skipping water lattice points too close to a surfactant bead (same "place on a grid, delete overlaps" approach GROMACS's own `solvate`/`insert-molecules` tools use). This is the function a real production CG self-assembly run should use, not `build_openmm_system()` (which stays as the small, fast, no-solvent smoke-test builder it always was). **Next concrete step**: benchmark real throughput on this system size (steps/second on OpenMM's CPU platform, not the slow Reference platform used for the small demo) and launch an actual production-scale run sized and timed like the atomistic route's real 10 ns campaign.

**Same-day follow-up — production run launched, benchmarked, and a real infrastructure issue hit (the same class already documented for SurfQM's Paper 2 batch, now confirmed to affect this repo too).** `examples/run_cg_production.py` built: 150 linear SDS-analog surfactants + real explicit MARTINI water in a 15 nm box (26,546 total particles), benchmarked on this machine's CPU platform at **107 ns/day** -- far faster than the atomistic route's 24-36 ns/day, as expected for CG's cheaper per-step cost and larger timestep. Launched targeting an ~11h/~49ns run. Real early signal, genuinely promising: by t=5.5 ns, the largest aggregate had already reached **18 molecules**, more than double the atomistic route's 9-molecule ceiling at 10 ns with fewer total monomers available -- consistent with CG force fields' well-known faster effective aggregation kinetics, not a red flag.

**Hit the exact "harness kills long Windows background processes" issue already documented for SurfQM's Paper 2 batch** (`H:\CodeProjects\SurfQM\ROADMAP.md`: "the harness itself killed the Windows batch process after ~30 min"). The tool-tracked background process was killed partway through (~3.4h in this case, not ~30 min, but the same underlying class of issue). No data lost -- the run's own checkpointing (binary checkpoint + append-only JSONL snapshot log every 500 ps) meant resuming was clean. Fixed the same way SurfQM's script was fixed: relaunched via a genuinely OS-detached process (PowerShell `Start-Process`, not the harness's own background-command tracking). Also found and fixed a real, if harmless, bookkeeping bug surfaced by this: the original `--resume` logic always started counting steps from 0 instead of reading the log's last `steps_done`, so a resumed run would silently run `--total-ns` worth of *additional* simulation on top of whatever the checkpoint already had, rather than stopping at the intended total -- not a correctness bug (the physics/checkpoint loading itself was fine), just imprecise run-length bookkeeping, now fixed for future resumes.

**2026-09-04 -- CG production run finished its planned 100 ns; largest aggregate plateaued at 33 while still visibly reshaping, so extended cross-machine to 200 ns.** Full run completed cleanly (5,000,000 steps, 14.53h wall time on Windows). `largest_aggregate_size` held flat at 33 from ~85 ns onward, but `largest_aggregate_rg_nm` kept climbing the whole time (10.78 nm at 80 ns -> 10.86 nm at 94 ns -> 11.09 nm at the final frame) -- constant monomer count with still-growing Rg means the aggregate was still reshaping, not settled. Extended by transferring `production.chk` to the Ubuntu machine and resuming there with `--total-ns 200` (another 100 ns beyond the checkpoint). **Real cross-machine checkpoint portability confirmed working**: both machines happened to have the exact same OpenMM dev build (`8.6.0.dev-c6173db`, conda-forge's current snapshot at transfer time), and the checkpoint loaded and resumed cleanly with no errors -- not something to assume works in general (OpenMM's own docs warn checkpoints can be platform/version-specific), but verified true here, not guessed. One real gotcha hit and fixed along the way: resuming without an explicit `--total-ns` auto-picks a target from a fresh benchmark on the new machine, which can come out *smaller* than the step count already reached from the checkpoint -- the run then immediately reports "DONE" without integrating a single further step. Fixed by always passing an explicit absolute `--total-ns` target on resume, not relying on the auto-pick. Extension launched and running as of this entry; result pending.

**2026-09-04 -- 100-molecule atomistic run analyzed: a real, quantified, honestly-mixed result, not a simple "bigger system, bigger micelle."** The scaled-up 100 SDS-anion/100 Na+ atomistic run (20 ns, GROMACS, launched to test whether more monomers reach past the 20-molecule run's {13,7} ceiling) finished cleanly and was analyzed with the existing PBC-aware `atomistic_analysis.py` (401 frames at 50 ps resolution). Late-run window (t=15-20 ns, 101 sampled frames): the dominant, modal state is a **16-molecule aggregate, present in 80% of frames** (81/101), alongside several smaller co-existing aggregates (recurring pattern: two-to-three 13-mers, one 11-mer, one 15-mer, one 5-6-mer -- consistently summing to the full 100-molecule population). Real, non-trivial improvement over the 20-molecule run's ceiling of 13. But the raw per-frame numbers also show frequent excursions to 27-30, and one single-frame spike to 45, at the *same* modal-16 aggregate's position in the distribution -- checked whether this is a genuine, repeated merge-and-split cycle or a clustering-cutoff artifact: the distribution reverts to the exact same steady sub-cluster sizes within 1-2 sampled frames each time (e.g. t=14950 already at 29, t=15050 spikes to 45, t=15150 is back to the same {29,13,13,13,11,5} pattern as before the spike) -- too fast and too exact a reversion to be a real, sustained physical merger breaking back apart; more consistent with transient bridging contacts between spatially adjacent-but-distinct aggregates crossing the 0.5 nm clustering cutoff for one frame without a genuine, lasting coalescence. Honest characterization: real progress toward micelle-scale aggregation (16 vs. 13, plus a cluster of several aggregates in persistent close proximity, not fully independent), but not yet a single unified micelle and not yet at the real SDS Nagg range (~45-54, Bales et al. 1998) -- still short on both molecule count and simulation time for that, same honest conclusion pattern as the 20-molecule run. Raw log: `examples/analysis_100mol_20260904.log`.
